import { CheckSquare } from 'lucide-react';
import PlaceholderPage from './PlaceholderPage';
export default function ApplicationsPage() {
  return (
    <PlaceholderPage
      title="Applications"
      description="Track application status — draft, ready, applied, interview, offer, rejected."
      icon={CheckSquare}
      phase="Phase 11 — Application tracking"
    />
  );
}
