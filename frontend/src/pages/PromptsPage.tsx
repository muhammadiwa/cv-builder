import { Brain } from 'lucide-react';
import PlaceholderPage from './PlaceholderPage';
export default function PromptsPage() {
  return (
    <PlaceholderPage
      title="AI Prompt Manager"
      description="View, edit, test, and version all internal AI prompts (parser, analyzer, matcher, generator, scorer, etc)."
      icon={Brain}
      phase="Phase 12 — AI Prompt Manager"
    />
  );
}
