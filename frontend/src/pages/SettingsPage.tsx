import { Settings as SettingsIcon } from 'lucide-react';
import PlaceholderPage from './PlaceholderPage';
export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      description="LLM provider toggles, environment, data location, security, and more."
      icon={SettingsIcon}
      phase="Phase 12 — Settings polish"
    />
  );
}
