import { Mail } from 'lucide-react';
import PlaceholderPage from './PlaceholderPage';
export default function CoverLettersPage() {
  return (
    <PlaceholderPage
      title="Cover Letters"
      description="Generated cover letters for each job application. Editable, exportable to PDF or DOCX."
      icon={Mail}
      phase="Phase 9 — Cover letter"
    />
  );
}
