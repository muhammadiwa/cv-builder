import { Layers } from 'lucide-react';
import PlaceholderPage from './PlaceholderPage';
export default function TemplatesPage() {
  return (
    <PlaceholderPage
      title="Templates"
      description="ATS-safe CV and cover letter templates. All single-column, no graphics, selectable text."
      icon={Layers}
      phase="Phase 6 — Templates (bundled with CV generator)"
    />
  );
}
