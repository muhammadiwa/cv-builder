'use client';

export default function SettingsPage() {
  return (
    <div className="max-w-2xl mx-auto py-8 px-4">
      <h1 className="text-2xl font-display font-bold text-foreground mb-8">Pengaturan</h1>
      <p className="text-muted-foreground">Settings page — connected to API endpoints. UI components to be built in UX polish stories.</p>
    </div>
  );
}
